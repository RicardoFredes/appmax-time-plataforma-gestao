node_modules
dist
.DS_Store
*.local

# Segredos do sync
.env
# Segredos locais da Pages Function (wrangler pages dev)
.dev.vars

# Dados gerados (regenerados via `pnpm sync`)
public/data/*.json

# TS build info
node_modules/.tmp

# Wrangler / Cloudflare
.wrangler
!public/data/.gitkeep
